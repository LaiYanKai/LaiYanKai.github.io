"use strict";

const NewMapSource = {
    Manual: 0,
    Template: 1,
    length: 2,
};
Object.freeze(NewMapSource);

const NewMapKeep = {
    Keep: 0,
    Discard: 1,
    length: 2,
};
Object.freeze(NewMapKeep);